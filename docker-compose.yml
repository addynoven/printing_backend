services:
  mongo:
    image: mongo:7
    container_name: poms-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - poms_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: poms

volumes:
  poms_mongo_data:
